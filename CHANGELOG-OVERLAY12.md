## Overlay 12: Live Leaderboard + Admin Export + Audit Viewer
- Public leaderboard switched from mock-only to live data (with fallback to mock).
- New `leaderboard-get` function aggregates live team scores.
- Admin exports added (CSV + JSON) via `admin-export`.
- Admin audit history viewer added via `admin-audit-list`.
